# yaml-wizzard
You describe what your automation should do in plain language. The skill generates valid Home Assistant YAML, validates it structurally, and outputs it as a `.yaml` file. It can also explain existing automations in plain language and debug broken configs.
